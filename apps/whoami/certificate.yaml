apiVersion: cert-manager.io/v1
kind: Certificate
metadata: 
  name: whoami-tls
  namespace: whoami
spec:
  secretName: whoami-tls
  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - whoami.nerv.local