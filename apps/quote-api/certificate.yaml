apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: quote-api-tls
  namespace: homepage
spec:
  secretName: quote-api-tls
  issuerRef:
    name: selfsigned-cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - quote.nerv.local