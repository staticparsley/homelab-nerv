apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: quote-api
  namespace: homepage
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: quote-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s