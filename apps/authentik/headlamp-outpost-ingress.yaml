apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: headlamp-authentik-outpost
  namespace: authentik
spec:
  ingressClassName: nginx
  rules:
    - host: headlamp.nerv.local
      http:
        paths:
          - path: /outpost.goauthentik.io
            pathType: ImplementationSpecific
            backend:
              service:
                name: authentik-server
                port:
                  number: 80